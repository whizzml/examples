{
  "name": "Join two datasets on some fields",
  "description": "Basic script to use the join-datasets procedure to join two datasets on a list of fields",
  "kind": "script",
  "source_code": "script.whizzml",
  "imports": ["../aggregations"],
  "inputs": [
    {
      "name": "dataset",
      "type": "dataset-id",
      "description": "Dataset ID"
    },
    {
      "name": "join-fs",
      "type": "list",
      "description": "List of fields to be used in the join"
    },
    {
      "name": "join-type",
      "type": "string",
      "description": "Type of join (join, left_join, right_join, inner_join, outer_join)"
    }
  ],
  "outputs": [
    {
      "name": "joined-dataset",
      "type": "dataset-id",
      "description": "Joined dataset ID"
    }
  ]
}
