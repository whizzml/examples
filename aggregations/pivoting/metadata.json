{
  "name": "Pivoting on fields in a dataset",
  "description": "Basic script to use the pivoting procedure to aggregate fields",
  "kind": "script",
  "source_code": "script.whizzml",
  "imports": ["../aggregations"],
  "inputs": [
    {
      "name": "dataset",
      "type": "dataset-id",
      "description": "Dataset ID"
    },
    {
      "name": "pivot-aggrs",
      "type": "map",
      "description": "Map of pivot aggregations per field"
    },
    {
      "name": "group-fs",
      "type": "list",
      "description": "List of fields in the group by section",
      "default": []
    }
  ],
  "outputs": [
    {
      "name": "pivoted-dataset",
      "type": "dataset-id",
      "description": "Pivoted dataset ID"
    }
  ]
}
