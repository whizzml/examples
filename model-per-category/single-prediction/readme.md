# Single predictions with category models

This scripts uses the result of
executing [Create category models](../create-category-models) to
perform a prediction.

See also [the package README](../readme.md).
